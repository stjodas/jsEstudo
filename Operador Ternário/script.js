

  var idade = prompt("Digite uma idade");
  var categoria = idade >= 18 ? "Adulto" : "juvenil"; 
  alert(categoria)
  
  // A instrução se equivale ao if...else
  
  if (idade >= 18) {
      let categoria = "Adulto";
  } else {
      let categoria = "Juvenil";
  }
